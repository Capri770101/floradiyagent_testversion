# 项目 Review —— 跳舞兰 · 花卉 DIY 智能体（flora_diy_agent）

> 审查时间：2026-08-31 ｜ 审查范围：后端（`backend/`）、智能体（`agent/`）、仓储与支付、工程配置
> 审查方式：逐文件静态阅读 + 跨模块一致性核对（未做运行时/浏览器验收，遵循项目 AGENTS.md 约定）
> 说明：前端 `H5/`（React 三端）本次仅从架构/约定层面评估，未做逐文件代码审查，详见文末「前端」一节。

---

## 一、总体评价

这是一个**完成度相当高、工程素养突出的全栈项目**：FastAPI 后端职责清晰（路由/存储/安全/机审分离），三层权限（user/merchant/admin）隔离设计到位，电商状态机与支付流程有真实的防篡改与幂等考虑，智能体自研 ReAct 主循环结构完整。整体可读性与注释质量在项目级里属于上乘。

但在「**异步架构一致性**」「**金额精度**」「**默认配置安全姿态**」三处存在需要在合并前处理的现实问题，其中「同步 LLM 调用阻塞事件循环」是相对严重且会直接影响线上并发能力的缺陷。

---

## 二、架构与工程化亮点（做得好的地方）

- **三端令牌/角色强隔离**：`security.resolve_strict` + `routers/common._require_admin/_require_merchant` + `_assert_order_owner` + `_assert_order_in_scope` 形成纵深。商家只能看绑定店铺数据、管理员端点全部 `_require_admin`，订单归属校验在路由层统一拦截（commerce.py:230、merchant.py:38）。
- **下单服务端权威取价**：`commerce.create_order` 完全忽略客户端传价，按 `repo.get_plan` 重算（commerce.py:471-475），从根上杜绝「改价下单」类 P0 篡改。
- **支付状态机可信来源唯一**：仅 `pay_notify` 验签后调 `mark_order_paid` 落库；`mark_order_paid` 带 `if row['paid']: return True` 幂等守卫（commerce.py:744），sandbox 直付与真实回调不会重复发积分。
- **订单状态机严格**：`ship_order/complete_order/cancel_order` 均校验前置状态，未支付不能直接发货（commerce.py:648-682）。
- **SSRF 防护扎实**（tasks.py:82-160）：协议白名单 + 主机白名单（含子域匹配）+ DNS 解析后私网/回环/保留 IP 拒绝 + **重定向逐跳重校验**（防「首跳白名单、二跳内网」绕过）+ 魔数识别真实图片格式。这是全项目最亮眼的安全实现。
- **限流 + fail-fast + 优雅降级**：内存/Redis 双限流器可切换；`api.py` lifespan 在 `APP_ENV=prod` 时强制 `AUTH_REQUIRED=true` 与 `JWT_SECRET`，否则拒绝启动。
- **ReAct 主循环有终止保障**：`for turn in range(1, max_iterations+1)`（agent.py:155）防止 LLM 死循环；工具产物驱动 UI 而非状态机硬锁，灵活且不易卡死。
- **生图产物文件名白名单**（chat.py:137）：`[A-Za-z0-9_.\\-]+` 防目录穿越。

---

## 三、安全问题（按严重级）

### P1 — 严重

**1. 同步 LLM 调用阻塞整个事件循环（高并发不可用）**
- 位置：`agent/engine/llm.py:103-112` 使用同步 `OpenAI` 客户端；`agent/agent.py:129 run()` 为 `async def`，被 `await self.run(...)` 在同一事件循环内调用（chat.py:54 `agent.arun`）。`call_llm` 内部还有 `time.sleep` 退避（llm.py:100）。
- 影响：单次 `/chat` 在等待 LLM（最长 `request_timeout=180s` × 重试）期间，**整个 uvicorn 事件循环被卡死**，其它所有请求（下单、商家履约等）全部排队阻塞。项目自身在 DB（commerce 注释「用 to_thread 避免阻塞」）、生图（chat.py:122「放在 asyncio.to_thread」）都遵循了异步化原则，唯独最长耗时的 LLM 调用没隔离——与既定架构原则自相矛盾。
- 修复：将 `call_llm` 用 `await asyncio.to_thread(call_llm, ...)` 包裹，或直接将 `OpenAI` 换成 `AsyncOpenAI`。这是上线前必须修的项。

**2. 默认配置为「开发不安全」：dev 模式可任意伪造身份**
- 位置：`security.get_current_user`（security.py:93-113）在 `AUTH_REQUIRED=false`（**默认值**，config.py:124）时，不校验令牌而直接采用请求体 `user_id`；`resolve_uid`（common.py:215）在 token 缺失时同样回退到 `user_id`。
- 影响：任何部署只要没把 `AUTH_REQUIRED=true`，攻击者即可在请求里填任意 `user_id` 冒充他人查看订单/地址/购物车。fail-fast 仅在 `APP_ENV=prod` 触发，staging/灰度若沿用默认配置则存在越权面。
- 修复：除本地联调外，将 `AUTH_REQUIRED=true` 作为非 prod 部署的最低要求，并在文档/启动日志中显式告警（目前 prod 告警有，非 prod 无强约束）。

### P2 — 一般（应修但不阻塞）

**3. 金额全程使用 `float`**（commerce.py 多处：`total_price`/`discount`/`amount`/payable 计算 476/707/751 等）
- 浮点累加/折扣存在精度风险（如 0.1+0.2≠0.3），对「支付金额/退款/对账」应采用 `Decimal` 或整数分。当前 demo 数据简单未暴雷，但进入真实交易前必须改。

**4. 两处 async 路由内直接同步调用 SQLite，阻塞事件循环**
- `backend/routers/commerce.py:298-300`（`order_aftersales_endpoint` 里 `get_conn().execute(...)`）；
- `backend/routers/merchant.py:425`（`my_merchant_application_endpoint` 里 `get_conn().execute(...)`）。
- 与项目「SQLite 走 to_thread/异步事务」的约定不符，且绕过存储层、不一致。应改为 `await commerce/list_user_aftersales` 或 `await asyncio.to_thread(...)`。

**5. 上传未做 MIME/魔数校验**（merchant.py:215-233）
- 仅校验扩展名 + 大小 + 机审。扩展名白名单 + Starlette 按扩展名定 content-type 基本挡住了 XSS，但缺魔数校验（一个伪装成 `.png` 的非图片仍可落盘）。建议加 PIL/`imghdr` 校验真类型。

### P3 — 建议

**6. `content_review_enabled=true` 但 `CONTENT_REVIEW_URL` 空、`fail_closed=false`（默认）时会静默放行**——生产若只开了开关忘了接 API，等于没审。建议生产默认 `fail_closed=true`。

**7. DNS Rebinding 残留风险**（tasks.py:_is_safe_image_url）：校验时解析 DNS 判私网，但 `_safe_get` 实际请求未重新校验最终连接 IP，理论上可被 DNS rebinding 绕过白名单。对 demo 足够，金融级可加「解析后 pin IP 直连」。

**8. `_client_ip` 信 `X-Forwarded-For` 首值**（common.py:101-106）——可被伪造用于绕过按 IP 限流。反向代理务必规范化 `XFF`，或在可信代理后取最后一跳。

**9. `GET /tasks/{task_id}` 不校验归属**（chat.py:106-113）——task_id 为随机 UUID，不可枚举，风险低；但鉴权模式下建议同时校验 owner。

---

## 四、正确性与健壮性

- **优惠券领取竞态**（commerce.py:81-105 `claim_coupon_offer`）：`stock>0` 检查与 `stock-1` 之间无行锁，高并发下可能超领（stock 变负）。有限量券场景需 `UPDATE ... WHERE stock>0` + 受影响行数判断。
- **积分并发**（add_points:137）：读余额→写在同一事务内，依赖 SQLite 写事务串行，基本安全，但多 worker 共享 SQLite 时仍可能竞争（见下文 DB 项）。
- **`config.py` 重复字段**（config.py:193-205 与 200-204 重复定义了 `wechatpay_v2_mch_id`/`wechatpay_v2_api_key` 等）：后者覆盖前者，纯属冗余且易误导。建议删除重复块。
- **知识库新模块未提交**：`agent/knowledge/generator.py`、`importer.py`、`backend/routers/knowledge.py`、`backend/storage/knowledge_db.py`、`models_knowledge.py`、`migrations/migrate_knowledge.py` 均为 untracked（未 `git add`）。属进行中的知识库特性分支，合并前需补齐测试与 lint。

---

## 五、智能体模块专项

- **ReAct 主循环**：结构清晰、`max_iterations` 兜底、工具产物→UI 推导逻辑（`_derive_ui`/`_validate_respond_data`）对 LLM 幻觉有防御（缺失核心字段则降级重建）。属于质量较好的实现。
- **工具注册表**：`@register_tool` 装饰器 + `TOOL_REGISTRY` + `generate_tool_manual` 自动注入，新增工具零改动 agent，设计优雅。`tools_enhanced.py` 已正确在 `agent.py:33` 导入触发注册。
- **提示注入面**（P3）：`save_memory` 写入的长期记忆与历史消息会拼进 system prompt（agent.py:145-147, 458-460）。恶意用户可借 `save_memory` 持久化注入指令。当前场景危害有限（不能触达外部动作/工具），但建议对长期记忆做结构化隔离、不进入系统指令区。
- **死代码/半成品**：`agent/skills/skill_budget_recommend.py`、`agent/skills/skill_flower_matching.py` 仅定义了字符串常量（`BUDGET_RECOMMEND_SKILL`/`FLOWER_MATCHING_SKILL`），**既未用 `@register_tool` 注册，也未被任何模块 import**（`skills/__init__.py` 仅扫描 `@register_tool`），且系统提示已硬编码了对应专业技能文本——这两份文件目前是无效代码，应删除或接入。

---

## 六、前端（H5）评估说明

本次未逐文件审查 `H5/`（约 1.1 万个文件）。基于 README/AGENTS.md 的架构约定评估：
- 三端三入口（`index.html`/`merchant.html`/`admin.html`）、令牌键隔离（`floradiy_token`/`floradiy_merchant_token`/`floradiy_admin_token`）设计正确。
- **建议补充的前端审查项**：① 智能体返回数据（`ui`/`data`）是否做了 XSS 转义（React 默认转义，但 `dangerouslySetInnerHTML` 若有使用需重点查）；② 生图/支付回调的客户端状态竞态；③ 三端共享 `src/` 组件是否存在越权调用错端接口；④ API 错误统一处理与令牌过期刷新。

---

## 七、工作树状态提醒

`git status` 显示大量**已修改未提交**（`backend/api.py`、`agent/agent.py`、`backend/storage/*` 等）与**未跟踪新文件**（知识库模块、`tools_enhanced.py`、`skill_*`）。建议：
1. 将「知识库」「增强工具」等特性分支与 `main` 隔离（遵循 GIT_WORKFLOW.md 的 `dev-*` 分支策略）；
2. 合并前跑通 `pytest -q -c misc/pyproject.toml` + `ruff` + `npx eslint` + `npx vitest run` + `npm run build`，并删除死代码与重复配置。

---

## 八、优先级修复清单

| 优先级 | 项 | 位置 | 动作 |
|---|---|---|---|
| P1 | 同步 LLM 阻塞事件循环 | llm.py:103 / agent.py:129 / chat.py:54 | `call_llm` 包 `asyncio.to_thread` 或换 `AsyncOpenAI` |
| P1 | 默认 dev 模式可伪造身份 | security.py:93 / common.py:215 | 非 prod 也强制 `AUTH_REQUIRED` + 文档告警 |
| P2 | 金额用 float | commerce.py 多处 | 改 `Decimal`/分 |
| P2 | 两处 async 内同步 SQLite | commerce.py:298 / merchant.py:425 | 改 to_thread 或走存储层 |
| P2 | 上传缺魔数校验 | merchant.py:215 | 加真类型校验 |
| P3 | 内容机审 fail-open | config.py:162 | 生产默认 `fail_closed=true` |
| P3 | 优惠券领取竞态 | commerce.py:81 | `UPDATE ... WHERE stock>0` + 行数判断 |
| P3 | config 重复字段 | config.py:193-205 | 删除重复 |
| P3 | 死代码 skill 文件 | skills/skill_budget_recommend.py, skill_flower_matching.py | 删除或接入 |
| P3 | XFF 限流可被伪造 | common.py:101 | 代理层规范化 |

---

*审查结论：架构与安全意识整体优秀，核心交易链路（取价/支付/状态机/越权）可靠；上线前须优先解决「同步 LLM 阻塞」与「默认身份可伪造」两项，并把金额计算切换到定点数。*
