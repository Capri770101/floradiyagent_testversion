services:
  flora-agent:
    build: .
    # 真实密钥从宿主机 .env 读取，不进镜像
    env_file:
      - .env
    ports:
      - "8000:8000"
    # 数据持久化（SQLite + 生图落盘），避免容器重建丢数据
    volumes:
      - ./data:/app/data
    restart: unless-stopped
