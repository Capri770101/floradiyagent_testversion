# flora_diy_agent 开发计划（当前阶段 · 可独立完成部分）

> 文档性质：规划与任务清单，**不涉及外部平台接入**。按用户两条红线编写，所有任务均可在当前仓库独立完成（无需微信支付商户号、快递 API、微信 appid 等外部资源）。
> 生成日期：2026-08-18 · 状态：待评审

---

## 0. 两条红线（最高约束，所有改动以此为准）

- **红线1 · 完成标准**：所有功能按**可上线**标准交付。暂无真实数据/接口/平台接入时，前端展示须先做出**完整 UI**，标准 DB 结构须先**建好**，缺失数据用 **seed 假数据**填充（上线前可整体清空重灌）。**不允许以「等后端接口 / 等真实数据」为由留空壳或写死占位**。
- **红线2 · 数据纪律**：前端**每一处展示字段都必须有真实后端数据来源**（来自 DB/接口，禁止前端硬编码假数据/写死常量）；前后端就字段建立契约，**逐页面审计字段对齐**；seed 数据只用于填充，不替代 schema 设计。改库结构须同步更新 seed 与前端契约。

推论：seed 假数据是**填充手段**而非**架构妥协**——schema 必须按真上线设计，字段来源必须真。

---

## 1. 现状体检（用红线衡量的真实差距）

> 说明：此前审查中「ShopDetail 公告/营业时间/地址空白」为**误判**——后端 `shops` 表早已含 `notice`/`hours`/`address` 三列且已灌值，前端也已渲染。以下是修正后的真实缺口。

### 🔴 红线1 违反（功能未达上线标准 / 空壳）

| # | 位置 | 问题 | 严重度 |
|---|------|------|--------|
| R1-1 | `ShopDetail.jsx`（商品区） | 后端 `get_shop`（`catalog.py:1458`）仅返回店铺元信息，**不聚合 `menu`（分类+商品）**；前端 `shop?.menu || []` 退化为空 → **进店看不到任何商品**，核心功能空壳 | P0 |
| R1-2 | 支付链路 | UI+状态机已有，但 `PAYMENT_PROVIDER=sandbox` 模拟成功 | 允许（等商户号） |
| R1-3 | 物流链路 | 时间线由 seed/商家手动维护，无实时位置 | 允许（等快递 API） |

### 🔴 红线2 违反（前端写死 / 字段无来源）

| # | 位置 | 问题 | 严重度 |
|---|------|------|--------|
| R2-1 | `ShopDetail.jsx:46` | 读 `shop.dist`，后端返回字段为 `distance_km` → 距离显示 `undefined` | P0 |
| R2-2 | `ShopDetail.jsx:61` | ~~读 `shop.delivery_time` 后端无此列~~ **已纠正**：`shops` 表**已有 `delivery_time` 列**(db.py:122) 且 `_row_to_shop` 用 `dict(row)` 返回，实际有值；此前"无此列"为误判。真实字段不匹配仅 R2-1 的 `shop.dist`→`distance_km` | 已达标 |
| R2-3 | `DiyDetail.jsx:66` / `Agent.jsx:421` | 商家名写死兜底 `'跳舞兰 定制'` / `'跳舞兰'`，应来自后端店铺数据 | P1 |
| R2-4 | `OrderConfirm.jsx:24` | `DELIVERY_OPTIONS`（配送时段）写死常量，应与后端配送配置同源 | P1 |
| R2-5 | `Admin.jsx` | ~~写死 CATEGORIES~~ **已纠正**：该页实际用 `merchantCategories()` 动态拉取分类（无写死），此前审查为陈旧 grep 误判。管理员专项缺口见下方「管理员界面」一节 | 已达标 |
| R2-6 | `Home.jsx:154` | 昵称末级兜底 `'Capri'` 字面量（前级已走 `/auth/me`，属低危兜底） | P2 |
| R2-7 | `Logistics.jsx:9-16` | 状态**文案正确**，但**颜色类名全部为 `bg-pink/10 text-pink`**，徽章失去视觉区分（done 应为绿、shipped 应为蓝/橙等） | P2（视觉瑕疵） |

### ✅ 已达标（无需动）
- 后端 80+ 端点与前端 50+ 调用 100% 对齐，无前端调了但后端 404 的缺口。
- `ShopDetail` 的 `notice`/`hours`/`address` 后端有列、前端已渲染。
- 订单/物流核心闭环（Orders/Logistics/Profile）真实数据驱动，演示账号 `capri_demo` 可跑通。

---

## 2. 阶段划分与任务清单

### 阶段0 · 建立前后端字段契约（基建，一次性，先于此阶段所有改动）

- **目标**：产出契约文档 `docs/FIELD_CONTRACT.md`，作为红线2的"标尺"。
- **内容**：逐页列出「前端展示字段 → 后端返回字段 → 是否已有来源 → 缺口」。先覆盖优先级页：ShopDetail / Home / DiyDetail / Agent / OrderConfirm / Admin / Orders / Logistics / Pay。
- **产出**：统一字段命名规范（距离一律 `distance_km`、配送时长 `delivery_time` 须入库、商家名 `merchant_name` 来自 shop 而非字面量）。
- **验收**：契约覆盖全部展示页；命名规范被后续阶段引用。

### 阶段1 · DB 按可上线标准补全（红线1）

| 任务 | 改动文件 | 内容 | 验收 |
|------|----------|------|------|
| T1-1 店铺商品菜单 | `storage/catalog.py`（`get_shop` / `_row_to_shop`）+ seed | 让 `get_shop` 聚合返回按分类组织的 `menu`：`[{category, items:[{id,name,price,desc,image}...]}]`，来源 `shop_plans`+`plans`+分类表；seed 灌示例 | ShopDetail 进店可见商品列表 |
| T1-2 配送时长列 | `storage/catalog.py`（`shops` 建表/迁移）+ seed | 加 `delivery_time` 列（如 "30分钟"）并灌值 | 后端 `get_shop` 返回 `delivery_time` |
| T1-3 方案展示字段 | `storage/catalog.py`（`plans`）+ seed | 评估并补齐 `ai_reason`（AI 推荐理由）等展示列，seed 填充 | 详情页有来源可渲染 |
| 迁移纪律 | `scripts/seed_*.py` | 改 schema 同时更新 seed 与迁移脚本，**保留现有数据**，提供可重灌命令 | 重灌后旧数据不丢、新列有值 |

### 阶段2 · 前端消除写死 + 字段对齐（红线2）

| 任务 | 改动文件 | 内容 | 验收 |
|------|----------|------|------|
| T2-1 字段改名对齐 | `ShopDetail.jsx` | `shop.dist`→`shop.distance_km`；`shop.delivery_time` 直接用（依赖 T1-2） | 距离/时长正常显示 |
| T2-2 商家名去字化 | `DiyDetail.jsx:66` / `Agent.jsx:421` | 商家名取自方案关联的店铺数据（或后端随方案返回 `merchant_name`），删字面量兜底 | grep 无 `'跳舞兰 定制'`/`'跳舞兰'` 作为商家名 |
| T2-3 配送时段同源 | `OrderConfirm.jsx` | `DELIVERY_OPTIONS` 改为从后端配送配置获取（T1 未含则先提为常量文件 `config/shop.js`，明确标注待后端接入） | 不重复散落字面量 |
| T2-4 分类同源 | `Admin.jsx` | `CATEGORIES` 提为与商家端一致的数据源（复用 `merchantCategories` 或新增后端分类接口） | 两处分类唯一来源 |
| T2-5 状态徽章配色 | `Logistics.jsx:9-16` | 按状态给差异化 `cls`（done=绿/shipped=蓝/paid=橙/canceled=灰），并抽为共享 `statusMeta` 工具供 Orders/Merchant/Profile 复用 | 徽章颜色区分正确、多页一致 |
| T2-6 删死代码 | `H5/src` | 移除零引用 `PlanCard.jsx`、`merchantShops` 等 | 构建无未用告警 |

### 阶段3 · 功能收口（红线1，按上线标准做 UI）

| 任务 | 内容 | 验收 |
|------|------|------|
| T3-1 支付 UI 完整化 | 支付/回调/状态页用 sandbox 验证全链路，UI 文案明确标注"生产接微信/支付宝" | 沙箱下单→支付→回调→订单终态全绿 |
| T3-2 物流 UI 完整化 | 时间线组件 + 状态机完善，seed 多状态订单验证 | 各状态物流页正常 |
| T3-3 搜索页 | 独立搜索 UI（复用 `listPlans(keyword)`），无独立搜索页时补 | 搜索可进、结果渲染 |
| T3-4 评价展示 | 接通 `/reviews` 的展示与发表入口 | 详情页可见评价 |

### 阶段4 · 一致性审计 + 测试（红线2保障）

| 任务 | 内容 | 验收 |
|------|------|------|
| T4-1 逐页契约走查 | 按阶段0契约逐页核对字段来源，输出走查表 | 无未对齐字段 |
| T4-2 前端测试扩充 | 每页渲染测试 + 字段来源断言（用 mock 后端），补齐 ESLint | `npm test` 通过、lint 0 error |
| T4-3 上线前清空脚本 | 提供 `scripts/clear_seed.py`（红线1允许清空重灌） | 一键清空 seed 数据 |

---

## 3. 执行顺序与优先级

1. **P0（先做，真空壳/字段断裂）**：阶段0契约 → T1-1(menu) → T1-2(delivery_time) → T2-1(字段对齐)。做完 ShopDetail 即同时达标红线1、红线2。
2. **P1（去写死）**：T2-2 / T2-3 / T2-4 / T2-5。
3. **P2（收口与视觉）**：T1-3 / T2-6 / T3-1~T3-4。
4. **P3（保障）**：T4-1~T4-3。

建议起点：**阶段0 + T1-1 + T1-2 + T2-1**（一条线解决最高优先真空壳）。

---

## 4. 验收口径（如何证明达标红线）

- **红线1 验收**：每个功能页都有完整 UI；`storage/catalog.py` 有标准表结构；`scripts/seed_*.py` 灌示例数据；无空壳页面、无"等接口"占位。
- **红线2 验收**：
  1. `docs/FIELD_CONTRACT.md` 覆盖全部展示页；
  2. 全仓库 grep 无 `跳舞兰 定制`/`Capri` 作为业务数据字面量、`DELIVERY_OPTIONS`/`CATEGORIES` 不再散落；
  3. 每页有渲染测试断言字段来自（mock）后端；
  4. 改库结构必同步 seed + 契约更新。
- **一致性门禁**：任何改动合并前，跑 `pytest`（后端）+ `npm test`（前端）+ `ruff`/`eslint`，全绿方可。

---

## 5. 不在本阶段范围（等外部接入，仅做 UI 与沙箱验证）

- 真实微信 v3 / 支付宝支付网关（需商户号、证书、回调域名备案）。
- 物流实时位置（需快递100 / 微信物流助手 API）。
- 微信登录 / 微信订阅消息（需 appid、公众号资质）。
- 千问开放平台接入（此前决策：先观望）。
- 图片上云 OSS/COS（当前 `merchantUpload` 落本地，生产需替换）。

---

## 6. 风险与注意

- **改 DB schema 必须同步 seed 与迁移**，避免破坏 `capri_demo` 等现有数据；提供重灌命令而非 `rm`。
- **字段改名需前后端同步**（契约先行），禁止单边改导致 `undefined`。
- **不动 WorkBuddy 内置微信支付 MCP 进程**（工具自身进程，非本项目）。
- 本计划所有任务均可在本地独立完成，不触发任何外部网络调用或凭据需求。
